# Sample Repository Notes

Nitely uses `pnpm exec vitest run` for unit tests and `pnpm run check` for
TypeScript validation. Prefer focused tests first, then run the full suite
before publishing a PR.
